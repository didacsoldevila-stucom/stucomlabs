services:
  wordpress:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      WORDPRESS_DB_HOST: ${WORDPRESS_DB_HOST}
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME}
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER}
      WORDPRESS_DB_PASSWORD_FILE: /run/secrets/db_password
      DB_PASSWORD: ${DB_PASSWORD}
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_MEMORY_LIMIT', '256M');
        define('WP_MAX_MEMORY_LIMIT', '256M');
        define('FORCE_SSL_ADMIN', true);
        define('DISALLOW_FILE_EDIT', true);
        define('AUTOMATIC_UPDATER_DISABLED', false);
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}

    volumes:
      - wp_html:/var/www/html

    labels:
      - traefik.enable=true
      # Router principal (normal)
      - traefik.http.routers.${SERVICE_NAME}.rule=Host(`${DOMAIN}`)
      - traefik.http.routers.${SERVICE_NAME}.entrypoints=websecure
      - traefik.http.routers.${SERVICE_NAME}.tls=true
      - traefik.http.routers.${SERVICE_NAME}.service=${SERVICE_NAME}
      - traefik.http.services.${SERVICE_NAME}.loadbalancer.server.port=80
      # Router SOLO para wp-login
      - traefik.http.routers.${SERVICE_NAME}-login.rule=Host(`${DOMAIN}`) && Path(`/wp-login.php`)
      - traefik.http.routers.${SERVICE_NAME}-login.entrypoints=websecure
      - traefik.http.routers.${SERVICE_NAME}-login.tls=true
      - traefik.http.routers.${SERVICE_NAME}-login.priority=100
      - traefik.http.routers.${SERVICE_NAME}-login.service=${SERVICE_NAME}
      - traefik.http.routers.${SERVICE_NAME}-login.middlewares=${SERVICE_NAME}-login-ratelimit
      # Rate limit DURO solo para login
      - traefik.http.middlewares.${SERVICE_NAME}-login-ratelimit.ratelimit.average=3
      - traefik.http.middlewares.${SERVICE_NAME}-login-ratelimit.ratelimit.burst=5

  wpcli:
    build:
      context: .
      dockerfile: Dockerfile.wpcli
    user: "33:33"
    working_dir: /var/www/html
    command: ["/scripts/bootstrap.sh"]
    restart: "no"

    environment:
      WORDPRESS_DB_HOST: ${WORDPRESS_DB_HOST}
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME}
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      HOME: /tmp
      DOMAIN: ${DOMAIN}
      WP_TITLE: ${WP_TITLE}
      STUDENT_CODE: ${STUDENT_CODE}
      STUDENT_NAME: ${STUDENT_NAME}
      STUDENT_SURNAME: ${STUDENT_SURNAME}
      STUDENT_EMAIL: ${STUDENT_EMAIL}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
      ELEMENTOR_PRO_URL: ${ELEMENTOR_PRO_URL}
      ELEMENTOR_PRO_LICENSE: ${ELEMENTOR_PRO_LICENSE}
    volumes:
      - wp_html:/var/www/html

volumes:
  wp_html: